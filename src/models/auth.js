export default class Auth {

    constructor(id,email, password, rememberMe) {
        this.id = id;
        this.email = email;
        this.password = password;
        this.rememberMe = rememberMe;
    }
}